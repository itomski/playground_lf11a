package de.lubowiecki.tag3;

public class ForTest {

    public static void main(String[] args) {

        // Beim Start wird das i auf 0 gesetzt
        // bei jedem Durchlauf wird geprüft, ob i kleiner 10 ist
        // Block wird ausgeführt
        // i wird um 1 erhöht
        for (int i = 0; i < 10; i++) {
            System.out.println(i);
        }

        // Aufgabe 1:
        // Schreibe eine Schleife, den Zähler jeweils um 2 erhöht

        // Aufgabe 2:
        // Schreibe eine Schleife mit 100 Durchläufen, die bei jedem Durchlauf
        // ausgibt, ob der Zähler eine gerade oder ungerade Zahl ist

    }
}
